# ===== OpsMonitor 部署环境配置 =====
# 修改后执行：docker-compose -f deploy/docker-compose.yml up -d

# 数据库配置
DB_HOST=192.168.40.111
DB_PORT=3306
DB_NAME=wgcloud
DB_USER=jk
DB_PASSWORD=QGTenSBETaAwyK4a

# 通信 token（Server 和 Agent 必须一致）
WGTOKEN=wgcloud

# Server 端口（宿主机映射端口）
SERVER_PORT=9999

# Agent 配置
# Server URL（Docker 内部通信用容器名，外部部署用实际 IP）
SERVER_URL=http://opsmonitor-server:9999
# Agent 绑定 IP（为空时自动获取）
BIND_IP=
