# OpsMonitor 宝塔面板 + Docker 部署教程（从零开始）

> 适用对象：在一台全新的 Linux 服务器上，用
>
> **宝塔面板**
>
> 管理 Docker，部署 OpsMonitor（opsmonitor 二开版）监控系统的完整流程。
> 预计耗时：30~60 分钟（视网络下载速度）。
> 配套文档：《DEPLOY.md》（1Panel / 原生部署与 JAR 部署），本教程与其配置完全兼容。



***

## 〇、架构与端口总览



```
&#x20;               ┌──────────────────────────────────────────────┐

&#x20;               │              Linux 服务器（宝塔面板）          │

&#x20;               │                                              │

&#x20;浏览器 ──9999──▶  \[容器] opsmonitor-server（Web + 告警引擎）     │

&#x20;               │        │                                     │

&#x20;               │        ├──▶ MySQL（宝塔安装 或 容器）           │

&#x20;               │        │                                     │

&#x20;               │  \[容器] opsmonitor-agent（监控本机，可选）      │

&#x20;               └──────────────────────────────────────────────┘

&#x20;被监控主机 ──每分钟上报──▶ opsmonitor-server（需装 agent 或 Docker agent）
```



| 端口   | 用途                | 是否对外开放              |
| ---- | ----------------- | ------------------- |
| 8888 | 宝塔面板              | 仅自己访问（建议改端口 + 安全入口） |
| 9999 | OpsMonitor Web 界面 | 对外（或仅内网）            |
| 3306 | MySQL             | **仅内网，切勿对公网开放**     |
| 9998 | WebSSH（可选功能）      | 仅内网                 |



***

## 一、安装宝塔面板（已装可跳过）

### 1.1 安装

用 SSH 工具（Xshell / FinalShell / Windows Terminal 的 `ssh` 命令）登录服务器，**root** 身份执行：



```
\# CentOS 7/8

yum install -y wget && wget -O install.sh https://download.bt.cn/install/install\_6.0.sh && sh install.sh ed8484bec

\# Ubuntu/Debian

wget -O install.sh https://download.bt.cn/install/install-ubuntu\_6.0.sh && sudo bash install.sh ed8484bec
```

安装完成后终端会打印（**务必保存**）：



```
外网面板地址: http://服务器IP:8888/安全入口xxxx

username: xxxxxxxx

password: xxxxxxxx
```

### 1.2 放行面板端口



* **云厂商安全组**（阿里云 / 腾讯云 / 华为云控制台）：放行 `8888`、`9999`，后续 3306 **不放行**。

* 宝塔会自动放行自身端口；`9999` 稍后在宝塔「安全」页放行。

### 1.3 登录后必做的安全设置

登录宝塔 → 面板设置：



1. 绑定宝塔账号（手机号注册即可）；

2. 修改面板端口（如 8888 → 28888）、修改安全入口、修改用户名密码；

3. 「面板 SSL」可选开启（自签证书，浏览器会提示不受信任，属正常现象）。



***

## 二、安装 Docker（宝塔软件商店）



1. 宝塔左侧菜单 → **软件商店**；

2. 搜索 **「Docker 管理器」**（有的版本叫 Docker/Docker 管理）→ 安装；

3. 安装完成后点「设置」：

* 确认状态显示「运行中」；

* 「镜像加速」页可填入国内加速地址（否则拉取镜像可能超时），例如阿里云个人加速地址（阿里云控制台 → 容器镜像服务 → 镜像加速器，免费申请）。

### 2.1 确认 docker compose 可用

宝塔首页 → 终端（或 SSH 进入），执行：



```
docker -v

docker compose version    # 新版

\# 或

docker-compose -v         # 旧版独立二进制
```



* 若 `docker compose version` 正常 → 后文统一用 `docker compose`；

* 若只有 `docker-compose` → 后文统一用 `docker-compose`。

* 两者都没有：`yum install -y docker-compose-plugin` 或 `apt install docker-compose-plugin`。

> 下文示例统一写 
>
> `docker compose`
>
> ，用旧版的同学请自行替换为 
>
> `docker-compose`
>
> （参数完全一致）。



***

## 三、准备 MySQL 数据库

**二选一**。推荐方案 A（宝塔装 MySQL），数据在宿主机，备份直观。

### 方案 A：宝塔软件商店安装 MySQL（推荐）



1. 软件商店 → 搜索 **MySQL** → 安装 **5.7 或 8.0**（内存 ≤2G 的小机器选 5.7，更省内存）；

2. 安装完成后 → 数据库页 → **添加数据库**：

* 数据库名：`opsmonitor`（与代码默认一致）

* 用户名：`opsmonitor`

* 密码：点「随机生成」并**复制保存**（示例中记作 `YourStrongPwd`）

* 访问权限：**本地服务器**（切勿选「所有人」，那会把 3306 暴露到公网）

* 字符集：**utf8mb4**

1. 记下连接信息：`127.0.0.1:3306`，库 / 用户 `opsmonitor`，密码 `YourStrongPwd`。

### 方案 B：用容器跑 MySQL（不想在宝塔装 MySQL 时）

SSH 终端执行：



```
docker run -d \\

&#x20; \--name opsmonitor-mysql \\

&#x20; \--restart always \\

&#x20; -e MYSQL\_ROOT\_PASSWORD=RootPwd123 \\

&#x20; -e MYSQL\_DATABASE=opsmonitor \\

&#x20; -e MYSQL\_USER=opsmonitor \\

&#x20; -e MYSQL\_PASSWORD=YourStrongPwd \\

&#x20; -v /www/opsmonitor/mysql:/var/lib/mysql \\

&#x20; -p 127.0.0.1:3306:3306 \\

&#x20; mysql:5.7 --character-set-server=utf8mb4 --collation-server=utf8mb4\_unicode\_ci
```

要点：



* `-p 127.0.0.1:3306:3306` 只绑定本机回环，公网无法访问；

* 数据落在 `/www/opsmonitor/mysql`（宿主机），重装容器不丢数据。

### 3.1 导入建表 SQL

**全新部署**只需导入 `sql/opsmonitor-MySQL.sql`（已包含 Webhook、TCP 端口监控、磁盘告警的全部字段，**不需要**再执行 upgrade 脚本）。

**方式一（图形化，推荐）**：



1. 宝塔 → 数据库 → 找到 `opsmonitor` 库 → 点「管理」打开 phpMyAdmin；

2. 选中 `opsmonitor` 库 → 「导入」→ 选择本地电脑上的 `sql/opsmonitor-MySQL.sql` → 执行；

3. 提示「导入成功」且左侧出现 `HEATH_MONITOR`、`MAIL_SET`、`SYSTEM_CONFIG` 等表即可。

**方式二（命令行）**：先把整个项目目录上传到服务器（见下一节），然后：



```
mysql -u opsmonitor -p'YourStrongPwd' -h 127.0.0.1 opsmonitor < /www/opsmonitor/opsmonitor-opensource/sql/opsmonitor-MySQL.sql
```

> **老版本升级**
>
> ：如果服务器上已经跑着旧版库，不要重新导入主 SQL，改为执行 
>
> `sql/upgrade-v3.6.3-webhook.sql`
>
> （增量加列，不会动已有数据）。



***

## 四、上传项目到服务器



1. 本地电脑把 `opsmonitor-opensource` 整个目录压缩成 zip（**不要包含&#x20;**`opsmonitor-server/target`**&#x20;等构建产物也可以，不影响**）；

2. 宝塔 → 文件 → 进入 `/www/opsmonitor`（没有就新建该目录）→ 上传 zip → 右键「解压」；

3. 解压后得到 `/www/opsmonitor/opsmonitor-opensource/`。



***

## 五、修改部署配置（deploy/.env）



1. 宝塔文件管理进入 `/www/opsmonitor/opsmonitor-opensource/deploy/`；

2. 目录里自带 `.env.example` 模板。复制一份命名为 `.env`（终端执行 `cp .env.example .env` 也可以）；

3. 用宝塔的文件编辑功能打开 `.env`，逐项修改：



```
\# 数据库配置 —— 与第三节创建的库对应

DB\_HOST=192.168.40.111     ← 改成数据库所在地址

DB\_PORT=3306

DB\_NAME=opsmonitor            ← 与 DB\_USER 必须一致

DB\_USER=opsmonitor

DB\_PASSWORD=YourStrongPwd  ← 第三节保存的密码

\# 通信 token —— Server 和所有 Agent 必须一致，建议改成随机长串

OMTOKEN=替换成32位以上随机字符串

\# Server 对外端口

SERVER\_PORT=9999

\# Agent 配置 —— 保持默认即可（Agent 容器通过 Docker 内网连 Server）

SERVER\_URL=http://opsmonitor-server:9999

BIND\_IP=
```

> ⚠️ 
>
> `deploy/.env`
>
>  含明文密码，已被 
>
> `.gitignore`
>
>  忽略，请勿提交到 git 或外发。

### 5.1 方案 A（宝塔 MySQL）必须注意的一件事

Server 是**容器**，它访问的 `127.0.0.1` 是容器自己，不是宿主机！所以：



* `DB_HOST` 不能写 `127.0.0.1`，要写**宿主机的内网 IP**（终端执行 `ip addr` 找 `eth0` 的地址，如 `172.x.x.x`）或 `host.docker.internal`（部分环境不支持）；

* 同时确认 MySQL 监听了外网接口：宝塔 → 软件商店 → MySQL → 设置 → 配置修改，确认 `bind-address = 0.0.0.0`（宝塔默认如此）且数据库访问权限为「本地服务器」时容器网段可能被拒 —— 若容器连不上库，把数据库访问权限临时改为「所有人」的同时，**必须在宝塔「安全」与云安全组里确保 3306 不对公网放行**，或给 MySQL 用户限定来源 IP 为 Docker 网段 `172.17.0.%`；

* 更省心的做法：方案 B（MySQL 也放容器里），`DB_HOST` 直接写 `opsmonitor-mysql`（容器名互通）。若选方案 B，请把 mysql 容器加进 compose 网络或改用下文 5.2 的扩展 compose。

### 5.2 （方案 B 专用）把 MySQL 一并纳入 compose

将 `deploy/docker-compose.yml` 的 `services:` 下追加（与 opsmonitor-server 平级）：



```
&#x20; opsmonitor-mysql:

&#x20;   image: mysql:5.7

&#x20;   container\_name: opsmonitor-mysql

&#x20;   restart: always

&#x20;   environment:

&#x20;     - MYSQL\_ROOT\_PASSWORD=RootPwd123

&#x20;     - MYSQL\_DATABASE=opsmonitor

&#x20;     - MYSQL\_USER=opsmonitor

&#x20;     - MYSQL\_PASSWORD=YourStrongPwd

&#x20;     - TZ=Asia/Shanghai

&#x20;   volumes:

&#x20;     - ./mysql-data:/var/lib/mysql

&#x20;   command: --character-set-server=utf8mb4 --collation-server=utf8mb4\_unicode\_ci

&#x20;   networks:

&#x20;     - opsmonitor-net
```

并且 server 服务的环境变量改为 `DB_HOST=opsmonitor-mysql`（.env 里改）。此时 server 不需要端口映射 MySQL，天然内网隔离，**最推荐**。



***

## 六、准备 Server 的 jar 包

容器构建时需要 `opsmonitor-server/target/opsmonitor-server-release.jar`（Dockerfile 会 COPY 它）。

### 方式一：本地打包后上传（推荐，服务器不需要装 JDK/Maven）



1. 本地开发机（要求 JDK 8 + Maven 3.6+）在 `opsmonitor-opensource/opsmonitor-server/` 目录执行：



```
mvn clean package -DskipTests
```

产物：`opsmonitor-server/target/opsmonitor-server-release.jar`



1. 宝塔文件管理上传 jar 到 `/www/opsmonitor/opsmonitor-opensource/opsmonitor-server/target/`（目录不存在就手动创建同名目录）。

> 本机 mvn 异常时的备用构建方式见《DEPLOY.md》附录（直接以 JDK java 启动 classworlds 离线编译，需先 package 一次打成 jar）。

### 方式二：服务器上直接打包

宝塔 → 终端：



```
\# 装 JDK8 与 Maven（CentOS 示例）

yum install -y java-1.8.0-openjdk-devel

cd /www/opsmonitor/opsmonitor-opensource/opsmonitor-server

mvn -v || (cd /www/opsmonitor && tar -xzf apache-maven-\*.tar.gz && export PATH=/www/opsmonitor/apache-maven-\*/bin:\$PATH)

mvn clean package -DskipTests
```



***

## 七、启动服务

宝塔 → 终端：



```
cd /www/opsmonitor/opsmonitor-opensource

\# 首次启动（--build 编译镜像；jar 已就位）

docker compose -f deploy/docker-compose.yml up -d --build
```

首次会拉取 `eclipse-temurin:8-jre` 等镜像（约 100MB），耐心等待。完成后验证：



```
docker ps                      # 两个容器 STATUS 应为 Up (healthy)

docker logs -f opsmonitor-server   # 看到注入 Spring、Tomcat started on port(s): 9999 即成功，Ctrl+C 退出
```

### 7.1 放行 9999 端口（两处都要）



1. 宝塔 → 安全 → 添加端口规则 → 放行 `9999`（TCP）；

2. 云厂商控制台安全组 → 同样放行 `9999`（TCP）。

### 7.2 访问验证

浏览器打开：



```
http://服务器IP:9999/opsmonitor/login/toLogin
```



* 默认账号 `admin` / `111111`；

* **登录后第一件事**：系统配置页把密码改成强密码；

* 第二件事：邮件告警配置页配置 SMTP / 钉钉 / 企业微信 Webhook（均支持加签）。

### 7.3 验证数据库连通（连不上时排查）



```
docker exec -it opsmonitor-server sh

\# 容器内

curl -s http://localhost:9999/opsmonitor/login/toLogin -o /dev/null -w "%{http\_code}\n"   # 期望 200

exit

docker logs opsmonitor-server 2>&1 | grep -i "Communications link failure"   # 有输出=连不上 MySQL
```

连不上的常见原因（按概率排序）：



1. `.env` 的 `DB_HOST` 写了 `127.0.0.1`（见 5.1）；

2. MySQL 用户只允许 `localhost` 来源 —— 授权容器网段：`GRANT ALL ON opsmonitor.* TO 'opsmonitor'@'172.17.0.%' IDENTIFIED BY 'YourStrongPwd'; FLUSH PRIVILEGES;`

3. 密码错（`DB_PASSWORD` 带了引号外的空格）。



***

## 八、Agent 部署（监控其他主机）

### 8.1 监控宝塔这台服务器本身

`deploy/docker-compose.yml` 里已包含 `opsmonitor-agent` 容器（随 `up -d` 一起启动了），无需额外操作。Docker 方式监控容器自身环境，**宿主机磁盘等指标可能不完整**（见 DEPLOY.md Q5），要完整监控宿主机建议用 8.2 的裸装方式。

### 8.2 监控其他 Linux 主机（裸装 agent，推荐）

在被监控主机上（root，需 JRE 8+）：



```
\# 1. 安装 JRE（已装 java 可跳过）

yum install -y java-1.8.0-openjdk    # 或 apt-get install -y openjdk-8-jre

\# 2. 创建目录

mkdir -p /opt/opsmonitor-agent/config && cd /opt/opsmonitor-agent

\# 3. 上传 agent 包：本地 mvn package 产物 opsmonitor-agent/target/opsmonitor-agent-release.jar

\#    上传到 /opt/opsmonitor-agent/，同目录建 config/application.yml（内容见下）
```

`config/application.yml` 关键内容（完整模板可参考 `deploy/AGENT-INSTALL.md`）：



```
base:

&#x20; # Server 端访问地址（含端口，不含 /opsmonitor 上下文路径）

&#x20; serverUrl: http://服务器IP:9999

&#x20; # 本机实际 IP，每台被监控主机必须唯一，不能写 127.0.0.1

&#x20; bindIp: 被监控主机IP

&#x20; # 通信 token，必须与 deploy/.env 的 OMTOKEN 完全一致

&#x20; omToken: 与Server一致的token
```

启动（后台运行）：



```
nohup java -server -Xms256m -Xmx512m -jar opsmonitor-agent-release.jar >/dev/null 2>&1 &
```

systemd 自启模板（`/etc/systemd/system/opsmonitor-agent.service`）：



```
\[Unit]

Description=OpsMonitor Agent

After=network.target

\[Service]

Type=simple

WorkingDirectory=/opt/opsmonitor-agent

ExecStart=/usr/bin/java -server -Xms256m -Xmx512m -jar opsmonitor-agent-release.jar

Restart=on-failure

\[Install]

WantedBy=multi-user.target
```



```
systemctl daemon-reload && systemctl enable --now opsmonitor-agent

systemctl status opsmonitor-agent   # active (running) 即正常
```

几分钟后在 Server 网页「主机监控」里应看到新主机上线。Agent 无需对公网开任何端口（只出站上报）。Windows 主机安装见 `deploy/AGENT-INSTALL.md`。



***

## 九、（可选）宝塔反代 + 域名 + HTTPS



1. 宝塔 → 网站 → 添加站点：域名填 `monitor.example.com`，不建数据库，PHP 版本选「纯静态」；

2. 站点设置 → **反向代理** → 添加：

* 目标 URL：`http://127.0.0.1:9999`

* 发送域名：`$host`

1. 站点设置 → SSL → Let's Encrypt 申请证书 → 开启「强制 HTTPS」；

2. 防火墙层面可以考虑：云安全组把 `9999` 对公网收回，只走 443 域名访问，更安全；

3. WebSSH（9998）如需使用，同样方式反代（WebSocket 需要 nginx `proxy_set_header Upgrade` 配置，宝塔反代模板已含）。



***

## 十、日常运维（宝塔化）



| 操作    | 做法                                                                                                                      |
| ----- | ----------------------------------------------------------------------------------------------------------------------- |
| 看日志   | 宝塔 Docker 管理器 → 容器 → opsmonitor-server → 日志；或 `docker logs --tail 200 -f opsmonitor-server`；文件日志在 `deploy/server/logs/` |
| 重启    | `docker compose -f deploy/docker-compose.yml restart` 或宝塔容器页重启按钮                                                        |
| 改配置   | 改 `deploy/.env` 后必须 `docker compose -f deploy/docker-compose.yml up -d`（重建容器才生效）                                        |
| 数据库备份 | 宝塔 → 数据库 → opsmonitor → 备份；或「计划任务」每天凌晨自动备份 + 异步发送到对象存储                                                                  |
| 升级版本  | 见下                                                                                                                      |

### 升级流程



```
cd /www/opsmonitor/opsmonitor-opensource

\# 1. 备份数据库（宝塔或命令行）

mysqldump -u opsmonitor -p'YourStrongPwd' opsmonitor > /www/backup/opsmonitor-\$(date +%F).sql

\# 2. 停服

docker compose -f deploy/docker-compose.yml down

\# 3. 上传新 jar 覆盖 opsmonitor-server/target/opsmonitor-server-release.jar

\# 4. 若有升级 SQL 先执行（如 sql/upgrade-v3.6.3-webhook.sql）

\# 5. 重建启动

docker compose -f deploy/docker-compose.yml up -d --build
```

`deploy/.env`、`deploy/mysql-data`（方案 B）、`deploy/server/logs` 都在容器外，`down` 不会丢数据。



***

## 十一、常见问题速查（宝塔场景）



| 现象                       | 原因与处理                                                                                                                                                   |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 浏览器打不开 9999              | 宝塔「安全」和云安全组两处都要放行；`docker ps` 确认容器 Up                                                                                                                   |
| 页面 502 / 健康检查一直 starting | `docker logs opsmonitor-server` 看报错；多半是 MySQL 连不上（见 7.3）                                                                                                |
| 登录报「验证码已失效」              | 服务器与浏览器时间不一致：宝塔 → 软件商店 → 系统防火墙外执行 `date` 核对，装 `ntpdate` 同步时间                                                                                            |
| 反代后验证码 / 静态资源 404        | 反代「发送域名」填 `$host`；清浏览器缓存                                                                                                                                |
| Docker agent 监控不到宿主机数据   | 属容器隔离所致，宿主机监控请用 8.2 裸装 agent                                                                                                                            |
| 镜像拉取超时                   | Docker 管理器 → 设置 → 配置国内镜像加速地址后 `systemctl restart docker`                                                                                                |
| 磁盘写满                     | 历史数据保留太长：系统配置页调低保留天数；`docker system prune` 清理悬空镜像                                                                                                       |
| 忘记 admin 密码              | 编辑 `deploy/server/config/application.yml`，设置 `base.admindPwd: 新密码`（该文件挂载在容器 /app/config/），然后 `docker compose -f deploy/docker-compose.yml up -d` 重建容器生效 |



***

## 十二、卸载



```
cd /www/opsmonitor/opsmonitor-opensource

docker compose -f deploy/docker-compose.yml down      # 停止并删除容器（数据保留）

docker rmi eclipse-temurin:8-jre                       # 释放镜像

\# 彻底删除：宝塔文件管理删除 /www/opsmonitor，数据库页删除 opsmonitor 库
```



***

*本文档基于 opsmonitor-opensource 当前源码结构（deploy/docker-compose.yml、deploy/server/Dockerfile、deploy/.env.example）编写，与《DEPLOY.md》互为补充。*