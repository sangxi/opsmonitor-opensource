# OpsMonitor 运维监控系统・二次开发文档（v2.0 现代化改造）

> 版本：v2.0 规划稿
> 日期：2026-09-03
> 基线：wgcloud v3.6.3 (Apache-2.0) 二开
> 文档性质：开发依据、改造方案、功能规划、验收基线



***

## 目录



1. [项目背景与目标](#1-项目背景与目标)

2. [现状盘点（基于 wgcloud v3.6.3 二开）](#2-现状盘点基于-wgcloud-v363-二开)

3. [总体架构与改造范围](#3-总体架构与改造范围)

4. [UI 现代化改造方案（对标参考图）](#4-ui-现代化改造方案对标参考图)

5. [监控大屏重构方案](#5-监控大屏重构方案)

6. [侧边栏「使用教程」模块设计](#6-侧边栏使用教程模块设计)

7. [功能增强清单（功能越多越好）](#7-功能增强清单功能越多越好)

8. [彻底去 wgcloud 化方案](#8-彻底去-wgcloud-化方案)

9. [技术选型与依赖](#9-技术选型与依赖)

10. [开发计划与里程碑](#10-开发计划与里程碑)

11. [验收标准](#11-验收标准)

12. [风险与注意事项](#12-风险与注意事项)



***

## 1. 项目背景与目标

### 1.1 背景

原版 **WGCLOUD** 存在以下商业化限制，导致本仓库基于开源版 v3.6.3 (Apache-2.0) 进行二次开发（二开）：



| 限制项  | 原版情况              | OpsMonitor 处理   |
| ---- | ----------------- | --------------- |
| 监控大屏 | 大屏为付费功能           | 自研大屏，免费开放       |
| 添加主机 | 限制 30 台           | 移除限制，支持数千台      |
| 品牌   | wgstart / wgcloud | 全新品牌 OpsMonitor |
| 界面   | AdminLTE 浅色旧风格    | 现代化深色主题         |
| 其他限制 | 部分功能收费 / 闭源       | 全部开放            |

### 1.2 v2.0 改造目标



1. **界面现代化**：整体 UI 从「能用」升级为「精美」，对标现代化监控大屏风格（深色侧边栏 + 彩色指标卡片 + 环形统计图 + 毛玻璃质感）。

2. **大屏精美重构**：替换粗糙的旧式大屏，重做为适配 1920×1080 及 4K 的高颜值可视化大屏。

3. **新增「使用教程」栏目**：侧边栏新增使用教程，分不同系统（Linux / Windows /macOS/ Docker / 宝塔 / 1Panel 等）讲解部署方式，并明确**各系统部署时需要挂载 / 准备的不同文件**。

4. **功能增强**：在保持稳定前提下，功能越多越好（新增拓扑、告警中心、报表、开放 API、多用户权限等）。

5. **彻底去 wgcloud 化**：界面与代码**不得出现** wgcloud /wgstart 的图片、名称、命名；逻辑与功能可与 wgcloud 一致，但对外呈现必须完全独立品牌。

### 1.3 不变量（铁律）



* 逻辑与功能可参考 wgcloud，但 **界面、静态资源、页面文案、Java 类名、包名、SQL、配置项、日志标识** 均不得出现 `wgcloud` / `wgstart` / `tianshiyeben`。

* 数据库库名 `wgcloud` 当前为兼容保留，v2.0 阶段完成迁移为 `opsmonitor`（见 §8）。

* 保持 Server（Spring Boot 2.6.6 + JDK8）+ Agent（OSHI 跨平台）双端架构，采集能力不降级。



***

## 2. 现状盘点（基于 wgcloud v3.6.3 二开）

### 2.1 技术栈



| 端      | 技术                                         | 说明                                 |
| ------ | ------------------------------------------ | ---------------------------------- |
| Server | Spring Boot 2.6.6                          | 端口 9999，context-path `/opsmonitor` |
| Server | JDK 8 / 11                                 | 低内存友好（-Xms256m -Xmx512m）           |
| Server | MySQL 5.5+ / MariaDB / PostgreSQL / Oracle | 默认 MySQL，utf8mb4                   |
| Server | Thymeleaf + AdminLTE 3                     | 服务端渲染，模板位于 `templates/`            |
| Server | G2（AntV）+ jQuery                           | 现有图表库                              |
| Server | MyBatis                                    | `resources/mybatis/mapper/`        |
| Agent  | Spring Boot + OSHI                         | 跨平台采集，默认每分钟上报                      |
| Agent  | port 9998                                  | WebSSH / 远程指令回调用                   |

### 2.2 目录结构（关键）



```
opsmonitor-opensource/

├── opsmonitor-server/            # Server 源码 + 产物

│   └── src/main/resources/

│       ├── templates/            # Thymeleaf 页面（改造重点）

│       │   ├── login/login.html  # 登录页（已紫粉渐变美化）

│       │   ├── dashView/         # 监控概要/公众看板

│       │   ├── common/           # aside(侧边栏)/header/navbar/footer

│       │   ├── host/ app/ heath/ mysql/ log/ mail/ systemConfig/

│       ├── static/

│       │   ├── css/opsmonitor-dark.css   # 现有深色主题（16KB）

│       │   ├── logincss/                 # 登录页资源

│       │   └── AdminLTE/                 # 前端框架（vendor）

├── opsmonitor-agent/             # Agent 源码 + 产物

├── sql/                          # 初始化与升级脚本

├── deploy/                       # Docker / 1Panel / 脚本部署

├── bin/                          # start/stop 脚本

├── daping/ demo/ docker/ GPU/    # 截图与素材

└── wgcloud-v3.6.3/               # 原版参考包
```

### 2.3 已完成改造（v1.x 成果）



* ✅ 品牌名 OpsMonitor、context-path `/opsmonitor`、通信 token `opsmonitor`

* ✅ 登录页现代紫粉渐变 + 自研 ECG 脉冲 logo（`logincss/logo.png`）

* ✅ 内页深色主题 `opsmonitor-dark.css`（侧边栏渐变、深色面板、亮色文字）

* ✅ 钉钉机器人（加签）、企业微信群机器人告警

* ✅ TCP 端口监控、HTTP 接口监控

* ✅ 告警阈值后台动态配置

* ✅ 验证码安全加固（SecureRandom + 一次性 + 有效期）

* ✅ Docker /docker-compose/ 1Panel 适配

### 2.4 待改造问题清单



| #  | 问题                  | 现状                                                                                                                          | 影响         |
| -- | ------------------- | --------------------------------------------------------------------------------------------------------------------------- | ---------- |
| P1 | **大屏粗糙**            | 旧式大屏布局简陋、分辨率适配差、无现代化视觉                                                                                                      | 展示档次低      |
| P1 | **残留 wgcloud 命名**   | 约 698 处残留（server 574 /agent 64 /sql 3 / 文档 50+），Java 包名 `com.wgcloud.*`、日志 `WgcloudServiceApplication`、配置项 `wgToken`、SQL 库名 | 违反品牌铁律     |
| P1 | 缺少「使用教程」栏目          | 侧边栏无教程入口                                                                                                                    | 用户上手难      |
| P2 | 内页统一性不足             | 部分列表 / 表单页仍偏 AdminLTE 原生观感                                                                                                  | 视觉不统一      |
| P2 | 图表库单一               | 仅 G2 部分图表                                                                                                                   | 大屏需要更强图表能力 |
| P3 | 无告警中心 / 报表 / 开放 API | 功能以原版为主                                                                                                                     | 功能不够多      |



***

## 3. 总体架构与改造范围

### 3.1 架构总览（v2.0）



```
&#x20;                        ┌─────────────────────────────────────┐

&#x20;                        │            Browser (PC / 大屏)       │

&#x20;                        │  登录页 · 内页(深色主题) · 监控大屏     │

&#x20;                        └───────────────┬─────────────────────┘

&#x20;                                        │ HTTP /opsmonitor

┌────────────────────────────────────────▼────────────────────────┐

│  OpsMonitor Server (Spring Boot 2.6.6, JDK8, port 9999)         │

│  ├─ 认证：登录/验证码/Session                                    │

│  ├─ 页面：Thymeleaf 模板（现代化主题）                            │

│  ├─ 大屏：DataScreenController + 聚合接口（JSON 一次取全量）       │

│  ├─ 教程：HelpController + help 表（可后台维护）                  │

│  ├─ 告警：邮件/钉钉/企微/WebHook 推送 + 告警中心                   │

│  ├─ 报表：定时报表 + 导出（Excel/PDF）                            │

│  └─ API：开放 REST API（token 鉴权）                             │

└───────────────────────────────┬────────────────────────────────┘

&#x20;                               │ HTTP + token

&#x20;                ┌──────────────▼──────────────┐

&#x20;                │  OpsMonitor Agent (OSHI)    │

&#x20;                │  Linux/Windows/macOS/Unix   │

&#x20;                └─────────────────────────────┘
```

### 3.2 改造范围（RACI）



| 工作包         | 涉及位置                                                | 类型   |
| ----------- | --------------------------------------------------- | ---- |
| UI 主题现代化    | `static/css/opsmonitor-dark.css`、全部 `templates/*`   | 前端   |
| 大屏重构        | 新增 `DataScreenController` + `templates/datascreen/` | 前后端  |
| 使用教程        | `HelpController` + `templates/help/` + `sql`        | 前后端  |
| 功能增强        | 各 service/controller + 新增表                          | 前后端  |
| 去 wgcloud 化 | 全部 java / 模板 / 静态 /sql/ 文档                          | 全局重构 |



***

## 4. UI 现代化改造方案（对标参考图）

### 4.1 设计语言（Design Token）

对标参考图：**深色侧边栏 + 高亮指标卡片 + 环形统计 + 现代留白**。



| Token        | 值                                                             | 用途      |
| ------------ | ------------------------------------------------------------- | ------- |
| 主背景          | `#0f1420` / `#121a2b`                                         | 全局底     |
| 面板背景         | `#1a2236`                                                     | 卡片 / 表格 |
| 侧边栏          | 深色渐变 `#0e1626 → #151f35`                                      | 导航      |
| 主色           | `#4f8cff`（蓝）                                                  | 按钮 / 高亮 |
| 辅色           | `#7c6cff` / `#22d3ee`                                         | 渐变 / 图表 |
| 成功 / 警告 / 危险 | `#22c55e` / `#f59e0b` / `#ef4444`                             | 指标状态    |
| 文字           | `#e6edf7`（主）`#8b96ad`（次）                                      | 可读性     |
| 圆角           | 10–14px                                                       | 卡片      |
| 阴影           | `0 8px 30px rgba(0,0,0,.35)`                                  | 层次      |
| 字体           | `-apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei"` | 中文友好    |

### 4.2 页面级改造清单



| 页面                     | 现状    | 改造动作                                            |
| ---------------------- | ----- | ----------------------------------------------- |
| 登录页                    | 已紫粉美化 | 微调：补充网格背景、呼吸动效、记住我 / 忘记密码入口                     |
| 侧边栏 aside.html         | 深色渐变  | 重构：分组导航、图标统一、折叠动画、**新增「使用教程」分组**、底部版本号          |
| 顶栏 navbar.html         | 基础    | 增加全局搜索框、通知铃铛（告警数角标）、用户下拉（改密 / 退出）               |
| 监控概要 dashView/index    | 深色卡片  | 重排卡片：渐变边框 + 图标徽章 + 趋势迷你线；环形图改 ECharts 美化        |
| 主机管理 host/list         | 列表    | 卡片化主机卡片视图 + 表格双模式切换；在线状态呼吸灯；快捷操作（SSH / 图表 / 指令） |
| 进程管理 app/list          | 表格    | 状态徽章、CPU / 内存占比进度条、批量操作                         |
| 数据监控 mysql/\*          | 表格    | 统一表格组件、数据源状态徽章、容量占比环                            |
| 服务接口 heath/\*          | 表格    | 健康状态卡片视图、响应时间趋势                                 |
| 日志 log/\*              | 表格    | 日志级别彩色标签、搜索高亮                                   |
| 系统配置 systemConfig/list | 表单    | 现代化设置面板（分段表单、开关控件）                              |
| 邮件告警 mail/view         | 表单    | 卡片化配置 + 测试按钮 + 推送记录                             |
| 404/500                | 默认    | 自定义品牌错误页                                        |
| 图表                     | G2    | 新增 ECharts 5（按需引入），统一暗色主题                       |

### 4.3 组件规范



* **统计卡片**：渐变底色卡片 + 大号数字 + 图标 + 「更多」链接 + 悬停上浮。

* **表格**：深色斑马纹、圆角表头、状态徽章、行操作按钮。

* **表单**：圆角输入框、聚焦高亮边框、分段式布局。

* **按钮**：主 / 次 / 危险三态 + 渐变主按钮。

* **弹窗**：毛玻璃遮罩 + 圆角卡片（基于 SweetAlert2）。

* **导航**：分组可折叠树形菜单，图标统一 FontAwesome。



***

## 5. 监控大屏重构方案

### 5.1 目标



* 对标现代运维大屏：**深色科技感 + 网格背景 + 高亮图表**。

* 支持 **1920×1080 / 2560×1440 / 4K** 自适应，兼容 `scale` 缩放。

* 独立无登录态访问（可投屏，token 校验），可自动轮播 / 全屏。

### 5.2 页面布局（16:9 栅格）



```
┌──────────────────────────────────────────────────────────────┐

│  标题：OpsMonitor 运维监控中心   系统时间   运行状态  全屏/刷新  │

├──────────────┬──────────────────────────────┬────────────────┤

│ ① 核心指标卡  │  ⑤ 主机资源分布（环形图）       │ ⑨ 告警信息TOP  │

│  主机总数     │   (CPU/内存/磁盘区间分布)       │   告警滚动列表 │

│  CPU总核数   ├──────────────────────────────┤               │

│  内存总和     │  ⑥ 资源使用率趋势（折线多线）    │ ⑩ 系统日志最新 │

│  存储总和     │   CPU/内存/磁盘/负载 实时趋势    │   日志滚动    │

│  累积告警     ├──────────────────────────────┤               │

│  在线主机     │  ⑦ CPU使用率 TOP10（横向柱状）   │ ⑪ 服务接口状态│

├──────────────┼──────────────────────────────┤   200/404/500 │

│ ② 系统类型分布│  ⑧ 内存使用率 TOP10（横向柱状）  ├──────────────┤

│  饼图        │                              │ ⑫ 网络带宽趋势 │

│ ③ 主机状态饼图│                              │   上下行实时   │

│ ④ 磁盘容量TOP│                              │               │

└──────────────┴──────────────────────────────┴──────────────┘
```

### 5.3 数据接口（聚合一次取全量）

新增 `GET /opsmonitor/datascreen/data`（token 鉴权），一次返回大屏全部数据，避免大屏轮询多个接口：



```
{

&#x20; "summary": {"hostTotal": 155, "cpuCore": 44, "memTotalG": 86.2, "diskTotalG": 1200, "alarmCount": 16961, "onlineHost": 120},

&#x20; "osType": \[{"name": "windows", "value": 60}, {"name": "centos", "value": 40}],

&#x20; "hostStatus": \[{"name": "正常", "value": 150}, {"name": "下线", "value": 5}],

&#x20; "cpuMemDist": \[{"name": "CPU和内存<50%", "value": 100}, {"name": "内存>90%", "value": 30}],

&#x20; "trend": {"time": \["10:00",...], "cpu": \[...], "mem": \[...], "disk": \[...], "load": \[...]},

&#x20; "cpuTop10": \[...], "memTop10": \[...], "diskTop": \[...],

&#x20; "alarmTop": \[...], "latestLog": \[...], "heathStatus": \[...],

&#x20; "netRate": {"time": \[...], "up": \[...], "down": \[...]}

}
```

### 5.4 大屏技术要点



| 项     | 方案                                          |
| ----- | ------------------------------------------- |
| 图表库   | ECharts 5（暗色主题自研）                           |
| 自适应   | CSS `transform: scale()` 按可视区等比缩放 + 1920 基准 |
| 数据刷新  | 前端 30s 轮询聚合接口（可配置）                          |
| 自动轮播  | 非核心区块定时切换（可选）                               |
| 全屏    | Fullscreen API + 大屏按钮                       |
| 无登录访问 | 独立 token（URL 参数 /header），可投屏                |
| 地图    | 不依赖地图，纯图表布局（规避商用地图授权风险）                     |



***

## 6. 侧边栏「使用教程」模块设计

### 6.1 需求

侧边栏新增 **「使用教程」** 栏目，覆盖：



1. **不同系统如何部署**（Server + Agent）；

2. **各系统部署需要挂载 / 准备的不同文件**；

3. 常见问题、告警配置、FAQ。

### 6.2 栏目结构



```
使用教程

├── 快速开始（新手引导：Server → Agent → 验证 → 告警）

├── Server 部署

│   ├── Linux（JDK8 + 原生 JAR / systemd）

│   ├── Windows（JAR + 服务）

│   ├── macOS（JAR + launchd）

│   ├── Docker / docker-compose

│   ├── 宝塔面板

│   └── 1Panel

├── Agent 部署（分系统）

│   ├── Linux（systemd 自启）

│   ├── Windows（WinSW 服务）

│   ├── macOS（launchd）

│   ├── Docker 内 Agent

│   └── 树莓派/ARM/其他

├── 数据库与初始化

├── 告警配置（邮件/钉钉/企微/WebHook）

├── 大屏使用说明

└── 常见问题 FAQ
```

### 6.3 各系统部署文件挂载清单（核心表格）



| 目标系统          | 需要准备的产物                | 需要挂载 / 放置的文件                                                                                                                                                  | 关键点                                   |
| ------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| **Linux**     | server jar + agent jar | `opsmonitor-server-release.jar`、`application.yml`、`opsmonitor-agent-release.jar`、systemd 服务文件（`/etc/systemd/system/opsmonitor-*.service`）                     | JDK8；日志目录 `./log`；token 与 Server 一致   |
| **Windows**   | server jar + agent jar | jar、`application.yml`、WinSW（`opsmonitor-agent.exe` + `.xml`）                                                                                                  | 服务注册；UTF-8 编码；bindIp 写实际 IP           |
| **macOS**     | server jar + agent jar | jar、`application.yml`、launchd plist（`~/Library/LaunchAgents/com.opsmonitor.*.plist`）                                                                          | Homebrew JDK8；PATH 配置                 |
| **Docker**    | 镜像 + compose           | `docker-compose.yml`、`.env`、`deploy/server/Dockerfile`、`deploy/agent/Dockerfile`、`deploy/server/config/application.yml`、`deploy/agent/config/application.yml` | 卷挂载 `logs`、`config`；agent `pid: host` |
| **宝塔面板**      | 项目压缩包                  | 网站目录 + Java 项目管理 + MySQL + `.env`                                                                                                                             | 面板防火墙放行 9999                          |
| **1Panel**    | 项目压缩包                  | 容器编排（Compose）+ 数据库 + `.env`                                                                                                                                   | 识别 `docker-compose.yml` 项目            |
| **树莓派 / ARM** | agent jar              | `opsmonitor-agent-release.jar` + `application.yml`                                                                                                            | 与 Linux 相同，注意架构                       |

> 教程页面将把上表做成「可点击系统 Tab → 展示对应步骤与文件清单」的交互式页面，并附可复制命令。

### 6.4 实现方案



* 数据模型：`help` 表（id、分类、标题、内容、排序、状态）+ `help_category` 表。

* 后台：`HelpController` 提供教程列表 / 详情；管理端可编辑（系统设置分组下新增「教程管理」）。

* 前端：`templates/help/list.html`（分类左侧 + 内容右侧）、`help/view.html`；代码高亮用 highlight.js。

* 权限：管理员可增删改；普通用户只读。

* 内置种子数据：首次部署自动写入上述全部系统部署教程。



***

## 7. 功能增强清单（功能越多越好）

### 7.1 监控能力增强



| # | 功能            | 说明                                |
| - | ------------- | --------------------------------- |
| 1 | 网络拓扑图增强       | 自动布局 + 链路流量标注 + 节点状态着色（已有基础，做现代化） |
| 2 | 端口监控批量导入      | CSV/JSON 批量添加端口、服务接口              |
| 3 | 进程白名单 / 黑名单   | 异常进程（未授权进程）告警                     |
| 4 | 磁盘 IO 与温度     | 细化磁盘 IO 监控、硬盘温度（已有 SMART 基础）      |
| 5 | GPU 监控（已有目录）  | 整合 NVIDIA 显存 / 温度 / 利用率到主机详情      |
| 6 | Docker 容器监控增强 | 容器列表、镜像、资源占用、异常重启告警               |
| 7 | 自定义监控项        | 用户自定义脚本 / 命令采集项（agent 扩展）         |
| 8 | 多时区 / 多语言     | 时间显示本地化，语言包 i18n（中 / 英）           |

### 7.2 告警中心



| # | 功能        | 说明                                 |
| - | --------- | ---------------------------------- |
| 1 | 告警中心页面    | 集中查看全部告警记录、状态（未处理 / 已处理 / 已忽略）、时间线 |
| 2 | 告警规则引擎    | 阈值 + 持续时长 + 恢复自动清除，避免抖动误报          |
| 3 | 告警升级      | 未处理 N 分钟后升级为紧急，推送到更多通道             |
| 4 | 告警去重 / 聚合 | 相同告警合并，收敛风暴                        |
| 5 | 告警通道扩展    | 飞书机器人、Server 酱、PushPlus、WebHook 通用 |
| 6 | 告警静默      | 维护窗口内静默指定主机 / 规则                   |

### 7.3 报表与审计



| # | 功能     | 说明                            |
| - | ------ | ----------------------------- |
| 1 | 定时报表   | 每日 / 每周 / 每月资源使用报告（邮件发送）      |
| 2 | 报表导出   | 主机清单、告警记录导出 Excel / PDF / CSV |
| 3 | 操作审计   | 登录、配置变更、指令下发等审计日志             |
| 4 | 数据保留策略 | 按天 / 月清理历史数据，可配置保留时长          |

### 7.4 管理与权限



| # | 功能           | 说明                               |
| - | ------------ | -------------------------------- |
| 1 | 多用户与 RBAC    | 管理员 / 操作员 / 只读 三角色，用户管理页         |
| 2 | 主机分组 / 标签    | 按业务分组、标签筛选、分组视图                  |
| 3 | 指令下发增强       | 目标分组批量执行、结果回显、执行历史               |
| 4 | 开放 REST API  | 主机列表 / 告警 / 指标查询，token 鉴权，供第三方集成 |
| 5 | 自定义系统名称 / 版权 | 已有，扩展到登录页副标题、大屏标题                |

### 7.5 体验与稳定性



| # | 功能          | 说明                      |
| - | ----------- | ----------------------- |
| 1 | 全局搜索        | 按主机名 / IP / 进程 / 端口快速检索 |
| 2 | 操作反馈        | 全局 loading、toast 提示统一   |
| 3 | 深色 / 浅色主题切换 | 提供浅色模式（可选）              |
| 4 | 数据导出        | 各列表页一键导出当前筛选            |
| 5 | Agent 心跳看板  | 上报延迟、掉线主机红名单提醒          |
| 6 | 备份工具        | 一键导出数据库备份脚本             |

### 7.6 M5 已实现清单（2026-09-03）

> 以下为 M5 阶段已落地并编译通过的增强功能，覆盖 7.2/7.3/7.4/7.5 中核心项。



| 模块              | 已实现内容                                                                                                                                         | 入口                                                     |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| **告警中心**        | ALARM\_RECORD 表 + 6 类告警接入（CPU / 内存 / 磁盘 / 服务接口 / 主机下线 / 进程下线）+ 恢复通知记录；去重（相同类型 + 主机 + 标题未处理不重复插入）；状态流转（未处理 / 已处理 / 已忽略）；类型筛选；级别徽章；详情弹窗；未处理计数接口 | `/opsmonitor/alarm/list`                               |
| **操作审计**        | AUDIT\_LOG 表 + 登录成功 / 失败 / 登出 + 配置变更 / 导出动作写入；操作类型筛选；导出 CSV；清空                                                                                | `/opsmonitor/audit/list`                               |
| **开放 REST API** | OPEN\_API\_TOKEN 表 + token 鉴权（请求头 X-API-Token）；主机列表 / 详情 / 告警 / 全局汇总 4 个接口，JSON 返回                                                            | `/opsmonitor/openapi/hosts` 等                          |
| **API 令牌管理**    | 新增 / 重新生成 / 删除令牌、过期时间、启用停用；默认演示令牌 `OpsMonitorApiToken2026`                                                                                    | `/opsmonitor/apiToken/list`                            |
| **CSV 导出**      | 主机清单 / 告警记录 CSV（UTF-8 BOM 防乱码、字段转义、导出写审计）                                                                                                     | `/opsmonitor/export/hosts`、`/opsmonitor/export/alarms` |
| **RBAC 扩展**     | user 角色可看告警 / 审计只读 + 导出，禁处理告警 / 改令牌 / 审计清空；test 角色可看告警只读                                                                                      | AuthRestFilter                                         |
| **全局搜索**        | 顶栏搜索按主机名模糊检索跳转主机列表                                                                                                                            | `/opsmonitor/dash/systemInfoList?hostName=`            |
| **菜单集成**        | 侧边栏新增「告警与安全」「审计与开放 API」分组 + 激活态；告警 / 审计 / API 页导出按钮                                                                                           | 侧边栏                                                    |

> M5 升级 SQL：
>
> `sql/opsmonitor-m5-upgrade.sql`
>
> （三张新表，IF NOT EXISTS 幂等，老库升级执行一次；已整合进 
>
> `init-db.sh`
>
>  自动导入）。

### 7.7 M6 补充实现清单（2026-09-03）

> 在 M5 基础上补充开发的两项增强功能，编译通过。



| 模块            | 已实现内容                                                                                                                                                                                 | 入口                        |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| **主机分组标签**    | SYSTEM\_INFO 增加 GROUP\_NAME（分组）/ TAGS（标签，逗号分隔）字段；主机列表新增分组列 + 标签徽章 + 分组筛选下拉；编辑弹窗支持分组 / 标签 / 备注；新增主机弹窗支持分组 / 标签；手动注册接口扩展                                                                | `/opsmonitor/host/list`   |
| **定时报表**      | REPORT\_LOG 报表记录表 + 每日 / 每周自动生成（每 5 分钟检查 reportTime 到点触发，幂等防重发）；HTML 邮件（主机资源概览 + 最近告警）；手动发送日报 / 周报；报表记录列表 / 预览 / 删除；系统配置面板（开关 / 时间 / 收件人）                                             | `/opsmonitor/report/list` |
| **SQL 恢复与升级** | 重建 `sql/opsmonitor-MySQL.sql`（库名 opsmonitor + SYSTEM\_INFO 补 DISK\_PER/GROUP\_NAME/TAGS）；新增 `sql/opsmonitor-enhance-upgrade.sql`（幂等加列 + REPORT\_LOG 表 + 报表配置项）；已整合进 `init-db.sh` 自动导入 | `sql/`                    |
| **菜单与权限**     | 侧边栏新增「报表中心」分组（激活态 101）；user 角色可看报表只读（/report/list、/report/detail）                                                                                                                     | 侧边栏 / AuthRestFilter      |



***

## 8. 彻底去 wgcloud 化方案

### 8.1 目标与铁律



* **界面**：登录页、侧边栏、大屏、所有页面不得出现 `wgcloud`/`wgstart`/`WGCLoud` 字样或相关图片。

* **代码**：包名、类名、变量、配置 key、日志、SQL 库名表名、jar 名、README / 文档，全面清理。

* **逻辑可同，品牌必新**。

### 8.2 清理清单



| 类别      | 现状                                  | 处理                                                             |
| ------- | ----------------------------------- | -------------------------------------------------------------- |
| Java 包名 | `com.wgcloud.*`                     | 重命名为 `com.opsmonitor.*`（IDE 重构 + 全部 import + mapper namespace） |
| 启动类     | `WgcloudServiceApplication`         | 改为 `OpsMonitorServiceApplication`                              |
| 日志标识    | `Started WgcloudServiceApplication` | 改 `Started OpsMonitorServiceApplication`                       |
| 配置项     | `wgToken` / `params.wgToken`        | 改 `token` / `omToken`，Agent `application.yml` 同步               |
| 数据库库名   | `wgcloud`                           | 迁移为 `opsmonitor`（§8.3）                                         |
| SQL 文件名 | `wgcloud-MySQL.sql`                 | 改 `opsmonitor-MySQL.sql`，内部 DROP/CREATE 库名同步                   |
| 静态资源    | `logincss/logo.png` 等               | 已替换为自研 logo；全局扫描确认无残留                                          |
| 模板      | 版权 / 外链 wgstart                     | 移除或替换为 OpsMonitor                                              |
| 文档      | README/DEPLOY/ 宝塔教程                 | 已基本清理，补充终扫                                                     |
| jar 名   | `opsmonitor-*-release.jar`          | 已合规，保持                                                         |

### 8.3 数据库迁移步骤



1. 备份旧库：`mysqldump -u root -p wgcloud > backup.sql`

2. 建新库：`CREATE DATABASE opsmonitor CHARACTER SET utf8mb4;`

3. 导入并改写：将 SQL 中 `wgcloud.` 前缀替换为 `opsmonitor.`

4. 更新配置：`.env` / `application.yml` /systemd 中的库名

5. 验证：启动后查询 `SHOW TABLES`，比对表数量一致

6. 保留兼容注释：提供 `upgrade-wgcloud2opsmonitor.sql` 一键迁移脚本

### 8.4 全局终扫脚本（验收用）



```
\# PowerShell 终扫（排除 AdminLTE 等 vendor 目录）

\$root = "D:\opsmonitor-opensource"

Get-ChildItem \$root -Recurse -File -Include \*.java,\*.html,\*.js,\*.css,\*.xml,\*.yml,\*.sql,\*.md -ErrorAction SilentlyContinue |

&#x20; Where-Object { $\_.FullName -notmatch 'AdminLTE|target\\\\|\\.git\\\\|node\_modules|\\.devdocs' } |

&#x20; Select-String -Pattern 'wgcloud|wgstart|tianshiyeben' -CaseSensitive:\$false |

&#x20; Group-Object Path | Select-Object Count, Name
```

> 验收标准：终扫结果 = 0（除 
>
> `wgcloud-v3.6.3/`
>
>  原版参考目录与 
>
> `.devdocs`
>
>  内部资料外，且该目录不参与构建发布）。



***

## 9. 技术选型与依赖



| 能力      | 选型                                 | 理由                                  |
| ------- | ---------------------------------- | ----------------------------------- |
| 图表      | ECharts 5（按需引入）                    | 大屏 / 趋势 / 环形能力最强，暗色主题成熟             |
| 现有图表    | 保留 G2                              | 避免大规模回归，逐步替换                        |
| 前端      | 继续 Thymeleaf + AdminLTE 3 + 自研 CSS | 服务端渲染稳定，改造成本可控                      |
| 代码高亮    | highlight.js                       | 教程代码块                               |
| 时间 / 日期 | 保留 My97DatePicker                  | 兼容现有表单                              |
| 导出      | Apache POI（Excel）+ 自研 PDF 模板       | 报表 / 清单导出                           |
| 告警推送    | HttpClient + 现有扩展                  | 飞书 / Server 酱 / PushPlus 均为 Webhook |
| 大屏无登录   | Token 参数鉴权                         | 投屏场景                                |
| 构建      | Maven + spring-boot-maven-plugin   | 现状保留                                |



***

## 10. 开发计划与里程碑

### 10.1 里程碑



| 阶段             | 周期    | 交付内容                                               | 状态                                                          |
| -------------- | ----- | -------------------------------------------------- | ----------------------------------------------------------- |
| M0 现状冻结        | 0.5 周 | 全量代码基线、SQL 备份、构建产物验证                               | ✅ 已完成                                                       |
| M1 去 wgcloud 化 | 1 周   | 包名 / 类名 / 配置 / SQL / 文档全清，终扫 = 0                   | ✅ 已完成（源码 / 文档 / 脚本 / 模板 = 0）                                |
| M2 UI 现代化      | 1.5 周 | 设计 Token、侧边栏 / 顶栏 / 卡片 / 表格 / 表单统一、登录页微调           | ✅ 已完成（深色主题 v2 + 顶栏搜索 + 教程菜单 + 首页卡片）                         |
| M3 大屏重构        | 1.5 周 | DataScreenController、聚合接口、ECharts 大屏、自适应 / 全屏      | ✅ 已完成（接口 + ECharts5 12 区块 + 全屏 + 30s 轮询）                    |
| M4 使用教程        | 1 周   | help 表 + HelpController + 教程页 + 种子数据（全系统部署 + 文件清单） | ✅ 已完成（9 分类 14 篇种子教程 + 清单 / 告警 / FAQ + marked/highlight 本地化） |
| M5 功能增强        | 2 周   | 告警中心、报表、RBAC、开放 API、分组标签、导出                        | ✅ 已完成（告警中心 / 操作审计 / 开放 API/RBAC/CSV 导出 / 主机搜索）              |
| M6 联调验收        | 1 周   | 全功能回归、大屏分辨率测试、性能压测、终扫                              | 🚧 功能开发完成（分组标签 / 定时报表）；联调环境待准备（本机无 MySQL、远程库未授权）            |

总计约 **8.5 周**（可并行：M2/M3/M4 前端为主，M5 后端为主）。

### 10.2 任务拆分（示例 Sprint）



| Sprint | 任务                                     |
| ------ | -------------------------------------- |
| S1     | 包名重构 + 终扫脚本；SQL 迁移脚本                   |
| S2     | Design Token CSS；aside/navbar 重构；登录页微调 |
| S3     | 统计卡片 / 表格 / 表单组件化；错误页                  |
| S4     | 大屏聚合接口 + 大屏页面 + 自适应                    |
| S5     | help 表 + 教程管理 + 教程页 + 种子数据             |
| S6     | 告警中心 + 报表导出 + RBAC + 开放 API            |
| S7     | 全局回归 + 性能 + 文档 + 发布包                   |



***

## 11. 验收标准



| 维度  | 标准                                                  |
| --- | --------------------------------------------------- |
| 品牌  | 界面与代码无 `wgcloud/wgstart/tianshiyeben`（终扫 = 0，除参考目录） |
| 界面  | 全站深色主题统一，对标参考图：卡片、环形图、侧边栏、顶栏                        |
| 大屏  | 1920×1080 / 4K 正常显示，图表动画流畅，数据 30s 自动刷新              |
| 教程  | 侧边栏可进入教程；覆盖 ≥6 种系统；每系统含「文件挂载清单」与可复制命令               |
| 功能  | §7 清单按里程碑逐项验收                                       |
| 稳定性 | 50+ 主机并发采集无数据丢失；大屏接口 P99 < 500ms                    |
| 兼容  | JDK8 可运行；MySQL 5.7/8.0 均可；原数据可迁移                    |



***

## 12. 风险与注意事项



1. **包名重构风险**：`com.wgcloud` 波及面大，务必用 IDE 重构 + Mapper namespace 同步 + 全量编译 + 启动回归，分模块提交。

2. **数据库迁移风险**：先备份、先在测试环境演练迁移脚本，再上生产。

3. **大屏性能**：聚合接口一次性查询需加索引、控制返回条数（TOP10）、Redis 缓存可选。

4. **图表库切换**：ECharts 与 G2 并存期间注意全局变量冲突（ECharts 挂 `window.echarts`，G2 挂 `window.G2`）。

5. **教程种子数据**：内置教程内容需随版本维护，建议抽取为独立 SQL 便于更新。

6. **vendor 目录**：AdminLTE 内资源不做品牌清理（第三方库版权），但不得在页面可见处引用其品牌文案。

7. **合规**：Apache-2.0 协议允许二开与闭源，但保留原版权声明于 LICENSE 中（不对外展示 wgcloud 品牌即可）。

8. **发布物**：最终发布包（zip）不包含 `wgcloud-v3.6.3/` 参考目录与 `.devdocs/` 内部资料。



***

*本文档为 v2.0 改造依据，随开发推进持续更新。*